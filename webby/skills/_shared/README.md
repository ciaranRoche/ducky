# Shared assets

Not a skill. Files here are referenced by other skills via `${CLAUDE_PLUGIN_ROOT}/skills/_shared/`.

- `daily-template.md`: pre-rendered copy of the vault's `80_System/Templates/Daily-Template.md` with Templater expressions replaced by placeholders. Section headings must stay identical to the vault template, every skill in this plugin anchors on them.
